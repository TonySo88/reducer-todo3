import React, { useState } from 'react';

export const TodoForm = (props) => {
    
    return (
        <form>
            <input 
                type="text"
                name="item"
                value={}
                onChange={}
            />
            <button>Add</button>
        </form>
    )
}